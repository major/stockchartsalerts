# Keep this as .dockerignore because GitHub Actions uses Docker Buildx even though
# the build file is named Containerfile.

# Git
.git
.gitignore
.github

# Secrets and local config
.env
.env.*

# Rust build output and coverage
target/
lcov.info
coverage.xml
htmlcov/

# Development
.vscode/
.idea/
*.swp
*.swo
*~

# CI/CD (already in git, no need in image)
.pre-commit-config.yaml
.coderabbit.yaml
codecov.yaml
renovate.json
